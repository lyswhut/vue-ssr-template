// state
const state = {
  name: '123',
}

// getters
const getters = {
  name: state => state.name,
}

// actions
const actions = {

}

// mitations
const mutations = {

}

export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations,
}
